<section id="projects">
	<h2>Projects</h2>
	<% projects.forEach(function(project) { %>
		<% if (!project.hidden) { %>
			<%- include('project-item.ejs', { project: project }) %>
		<% } %>
	<% }); %>
</section>