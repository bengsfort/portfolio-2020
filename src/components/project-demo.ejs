<% if (demo.video) { %>
<div class="demo-wrapper <%= demo.device %>">
	<!-- poster=project.feature + ".png" -->
	<div class="demo-inner">
		<video class="project-video" loop="loop">
			<source type="video/mp4" src="<%- demo.video %>" />
		</video>
	</div>
	<!-- img.project-image(alt=project.title, src=project.feature + ".png") -->
</div>
<% } %>